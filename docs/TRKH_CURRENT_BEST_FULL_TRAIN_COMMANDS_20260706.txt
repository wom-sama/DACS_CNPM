TRKH current-best commands for VS Code PowerShell
Created: 2026-07-06
Selected recipe preflight-verified: 2026-07-14
Last challenger audited: 2026-07-14
Precision deployment backend audit completed: 2026-07-14

CURRENT SELECTED VERSION

- Train base/checkpoint:
  runs\probe_v8_yolof_pairroute_teacherfocusbinary015_boundarydrop_bboxprior_120b_2e_20260701\checkpoints\best.pt
- Raw validation macro/class-1 F1: 0.884675 / 0.686047.
- Independent reload validation macro/class-1 F1: 0.882925 / 0.678261.
- Runtime softboost validation macro/class-1 F1: 0.888675 / 0.703030.
- Locked raw final-test macro/class-1 F1: 0.8865 / 0.6667.
- The 2026-07-11 30-epoch continuation stopped at epoch 5 and did not replace
  this checkpoint. The wrapper below therefore still resumes the selected
  keeper; it does not promote a rejected experimental checkpoint.
- The 2026-07-14 scratch run stopped at epoch 23. Its independent validation
  macro/class-1 F1 was 0.874172/0.654639, so it does not replace the keeper.
- A validation-locked two-checkpoint precision mode reached final-test
  macro/class-1 F1 0.887869/0.675676 and raised class-1 precision from
  0.597826 to 0.666667. It is now an OPTIONAL high-precision deployment mode
  on packaged PyTorch FP32 and ONNX Runtime CPU FP32. It is not a better
  single-model training recipe.
- Precision-package PyTorch replay passed all full-validation gates. ONNX CPU
  passed with max probability error 2.980232e-7 and zero decision mismatch.
  ONNX CUDA, TensorRT FP16, and TensorRT FP32 noTF32/O0 failed the frozen
  probability/decision gate. Do not use a TensorRT engine for this package as
  a certified backend.

WHY THESE COMMANDS ARE SAFE TO PASTE IN VS CODE

- Every command is one physical line and uses an absolute script path.
- The wrappers set the repository working directory themselves.
- Native Python processes are invoked directly. They are never piped through
  `2>&1 | ForEach-Object`, which caused the NativeCommandError in the supplied
  VS Code screenshot when Python wrote valid INFO logs to stderr.
- Every native process is checked with `$LASTEXITCODE`; a failed step stops the
  next step and writes pipeline_status.json.
- Training always uses -SkipFinalTest internally. Because the project test has
  already been opened historically, the explicit -RunFinalTest phase is now a
  retrospective project-test audit only; it cannot create confirmatory evidence
  or authorize scientific promotion. The wrapper opens it only after training,
  architecture trace, all validation audits, and the raw validation comparison
  gate pass. The gate requires both
  independent-reload macro F1 >= 0.882925 and class-1 F1 >= 0.678261. This
  compares the new checkpoint with the keeper through the same evaluator;
  0.884675/0.686047 remains the in-training anchor, not the reload gate.

1) OPTIONAL PREFLIGHT ONLY (no train, no test, no run directory)

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_current_best_full_pipeline.ps1" -RunName "preflight_current_best_$(Get-Date -Format yyyyMMdd_HHmmss)" -RunFinalTest -PreflightOnly

2) ONE-COMMAND FULL PIPELINE: TRAIN + VALIDATION AUDITS + HISTORICAL TEST + XAI

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_current_best_full_pipeline.ps1" -RunName "full_v8_yolof_current_best_30e_$(Get-Date -Format yyyyMMdd_HHmmss)" -RunFinalTest

The one command above runs, in order:

- exact current selected TRKH v8 recipe, max 30 epochs, patience 3, architecture trace;
- independent full validation for raw checkpoint and frozen softboost001 hook;
- validation prediction forensics, class-confusion audits, and boundary reviews;
- full validation robustness audit;
- raw and verifier-aware validation XAI with robustness probes and transition summaries;
- a validation promotion gate against the selected raw keeper;
- raw and verifier-aware historical-project-test evaluation only when that gate passes;
- historical-test prediction forensics, confusion audits, verifier-aware boundary review;
- verifier-aware historical-test XAI and transition summary;
- final artifact-retention audit;
- runs\latest_full_pipeline.json, which points export/video wrappers to the completed run.

If the gate fails, the same command still preserves all validation audits,
writes status `validation_gate_rejected`, skips the historical test, and does
not update the latest deploy pointer. Do not use historical-test outputs to
alter thresholds, losses, model selection, or a confirmatory claim. A new paper
claim requires a prospectively sealed source/time/site cohort and separate
authorization.

3) ONE-COMMAND OPTIONAL PRECISION PACKAGE + FULL VAL PARITY + ONNX CPU + XAI

Preflight only:

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_precision_ensemble_pipeline.ps1" -RunName "preflight_precision_ensemble_$(Get-Date -Format yyyyMMdd_HHmmss)" -PreflightOnly

Build and audit the locked package without reopening test:

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_precision_ensemble_pipeline.ps1" -RunName "precision_ensemble_certified_$(Get-Date -Format yyyyMMdd_HHmmss)"

This command hash-verifies both source checkpoints and the frozen validation
protocol, builds one self-contained checkpoint, runs exact full-validation
PyTorch parity, exports a fixed-batch three-input/six-output ONNX model, runs
full ONNX Runtime CPU parity, then runs member-specific native attention,
rollout, Grad-CAM, and robustness XAI on 12 locked validation cases per member.
It writes runs\latest_precision_ensemble.json only after every gate passes.
The historical project test is not rerun because its frozen result is already
recorded and this command performs no model/rule selection.

4) EXPORT THE SINGLE-CHECKPOINT PIPELINE TO ONNX + TENSORRT FP16 .ENGINE

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_export_engine.ps1"

The export wrapper reads runs\latest_full_pipeline.json and writes:

- <run>\deploy\model_fp32.onnx
- <run>\deploy\model_fp32_fp16.engine
- <run>\deploy\deploy_summary.json

Export the currently selected keeper directly, without first running command 2:

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_export_engine.ps1" -Checkpoint "D:\DataAI\AIEx\TRKH\runs\probe_v8_yolof_pairroute_teacherfocusbinary015_boundarydrop_bboxprior_120b_2e_20260701\checkpoints\best.pt"

The deploy module now blocks TensorRT export for a precision-ensemble package
by default because every tested GPU backend failed its declared parity gate.
The commands above remain valid for the selected single-checkpoint keeper.

5) TEST A VIDEO WITH THE SINGLE-CHECKPOINT TENSORRT ENGINE (separate command)

Replace D:\VIDEO\input.mp4 with the real video path:

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_test_video.ps1" -Source "D:\VIDEO\input.mp4" -SaveOutput

The video wrapper reads the latest checkpoint and default FP16 engine. It now
supports classification-only TensorRT engines; bbox outputs are not required.

6) TEST THE SAME VIDEO WITH SINGLE-CHECKPOINT PYTORCH (comparison/fallback)

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_test_video.ps1" -Source "D:\VIDEO\input.mp4" -UsePyTorch -SaveOutput

7) TEST THE OPTIONAL PRECISION PACKAGE WITH PYTORCH (separate command)

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\DataAI\AIEx\TRKH\scripts\run_trkh_test_video.ps1" -Checkpoint "D:\DataAI\AIEx\TRKH\runs\precision_ensemble_checkpoint_preflight_20260714\precision_ensemble.pt" -Source "D:\VIDEO\input.mp4" -UsePyTorch -SaveOutput

Image-only video does not provide the audited object bbox/mask inputs. The
precision package therefore labels this path unverified_full_frame_fallback;
use it for an operational video check, not as evidence that object-crop
validation/test metrics transfer to full frames. No precision-package
TensorRT video command is published until an engine passes the unchanged gate.

UPDATE POLICY

When a new version is judged better under the locked validation protocol, update
all of the following together before publishing a new command:

1. selected checkpoint and recipe defaults in run_trkh_current_best_full_pipeline.ps1;
2. the selected-version metrics and commands in this TXT file;
3. TRKH_5CLASS_RESEARCH_JOURNAL.md, TODO_TRKH_5CLASS.md, and the trkh-5class skill;
4. PowerShell parse, full-pipeline -PreflightOnly, precision-pipeline
   -PreflightOnly, engine -PreflightOnly, video -PreflightOnly, and focused tests.

Never update these commands from a smoke-only gain, a validation-oracle result,
or a test-tuned result.
